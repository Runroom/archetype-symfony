{% if page.cookies is not empty %}
    <script>
        PERFORMANCE_COOKIES = {{ page.cookies.performanceCookies|json_encode|raw }};
        TARGETING_COOKIES = {{ page.cookies.targetingCookies|json_encode|raw }};
        COOKIES_DEFAULT_DOMAIN = '{{ cookiesDefaultDomain }}';
    </script>
{% endif %}
