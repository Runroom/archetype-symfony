framework:
    secret: '%env(APP_SECRET)%'
    form: true
    csrf_protection: true
    templating:
        engines: [twig]
    session:
        handler_id: null
        cookie_secure: auto
        cookie_samesite: lax
