---
- name: Open on /vagrant by default
  lineinfile:
    dest: /home/vagrant/.bashrc
    state: present
    line: 'cd /vagrant'

- name: Install vendors
  composer:
    command: install
    working_dir: /vagrant
