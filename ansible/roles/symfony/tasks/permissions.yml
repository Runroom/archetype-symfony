---
- name: Set correct permissions
  acl:
    name: '/home/vagrant/{{ item }}'
    entity: www-data
    etype: user
    permissions: rwX
    default: yes
    recursive: yes
    state: present
  with_items:
    - cache
    - logs

- name: Set correct permissions
  acl:
    name: '/home/vagrant/{{ item }}'
    entity: vagrant
    etype: user
    permissions: rwX
    default: yes
    recursive: yes
    state: present
  with_items:
    - cache
    - logs
